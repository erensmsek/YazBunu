# --- Çekirdek (her platform, API modu dahil) ---
fastapi
uvicorn[standard]
python-multipart
python-dotenv
requests

# --- Lokal mod: transkript (yalnızca Apple Silicon) ---
# Platform marker'ı, mlx-whisper'ın Apple Silicon dışı kurulumlarda pip'i
# çökertmesini önler (diğer platformlar yalnızca API modunda çalışır).
mlx-whisper; platform_system == "Darwin" and platform_machine == "arm64"

# --- Çeviri (NLLB, yalnızca Lokal mod) ---
# Cross-platform ama büyük; server.py tembel import eder, API-modu kullanıcısı
# hiç yüklemez (mode=api iken translate.py import edilmez).
transformers
sentencepiece
torch

# --- Konuşmacı ayrımı (her zaman lokal, mod fark etmez) ---
# "Konuşmacı ayrımı" işaretlenirse API modunda bile çalışır (bkz. diarize.py) —
# bu yüzden torch'a ek olarak her platformda gerekli, yalnızca Apple Silicon'a
# özel değil.
pyannote.audio
